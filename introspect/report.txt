# introspect report
# generated: 2026-09-23 02:18:49Z
# commit: 850d72593dca17326536408c0d2d3d04a334e78f

=== FILE: art-core.js  LINES 8908-8950
8908 const FOUNDER_ACTIONS_BY_INDUSTRY = {
8909 ai_saas: [
8910 { key:"outreach", label:"\u{1F976} Cold Outreach Pitch", desc:"Pitch small prospects yourself \u2014 no reps needed." },
8911 { key:"consulting", label:"\u{1F4BC} Paid Implementation Gig", desc:"Bill a client for setup work. One-time cash." },
8912 { key:"network", label:"\u{1F91D} Work the Network", desc:"Founder meetups and referrals." },
8913 { key:"v18_demo", label:"\u{1F4BB} Run a Live Demo Day", desc:"Host a public demo. Shot at two clients at once." },
8914 { key:"content", label:"\u270D\uFE0F Write a Viral Post", desc:"Publish under your own name for buzz." },
8915 { key:"negotiate", label:"\u{1F911} Renegotiate a Vendor Contract", desc:"Trim a recurring cost." },
8916 ],
8917 retail: [
8918 { key:"v18_sidewalk", label:"\u{1F6CD}\uFE0F Work the Sales Floor", desc:"Spend the week selling in-store. Direct cash bump." },
8919 { key:"v18_supplier", label:"\u{1F4E6} Squeeze a Supplier", desc:"Negotiate better unit costs on your next restock." },
8920 { key:"v18_popup", label:"\u26FA Run a Weekend Pop-Up", desc:"One-time cash from a market stall." },
8921 { key:"network", label:"\u{1F91D} Court a Landlord", desc:"Better lease terms and a shot at a wholesale account." },
8922 { key:"content", label:"\u{1F4F8} Local Press & Social Push", desc:"Free foot traffic if it lands." },
8923 { key:"negotiate", label:"\u{1F911} Renegotiate a Vendor Contract", desc:"Trim a recurring cost." },
8924 ],
8925 restaurant: [
8926 { key:"v18_kitchen", label:"\u{1F468}\u200D\u{1F373} Work the Line Yourself", desc:"Cover shifts. Saves labor cash this month." },
8927 { key:"v18_tasting", label:"\u{1F37D}\uFE0F Host a Tasting Night", desc:"Fill seats and win regulars." },
8928 { key:"v18_critic", label:"\u{1F4DD} Invite a Food Critic", desc:"High risk, high reward reputation swing." },
8929 { key:"v18_catering", label:"\u{1F372} Take a Catering Job", desc:"One-time cash from an event." },
8930 { key:"network", label:"\u{1F91D} Work the Local Scene", desc:"Suppliers, chefs, and referrals." },
8931 { key:"negotiate", label:"\u{1F911} Renegotiate a Food Contract", desc:"Trim recurring food cost." },
8932 ],
8933 robotics: [
8934 { key:"v18_prototype", label:"\u{1F527} Bench-Build a Prototype", desc:"Personally shave time off the next unit." },
8935 { key:"v18_pilot", label:"\u{1F3ED} Land a Pilot Install", desc:"Pitch a plant manager on a paid pilot." },
8936 { key:"v18_grant", label:"\u{1F4DC} Chase an R&D Grant", desc:"Paperwork now, one-time non-dilutive cash if it lands." },
8937 { key:"v18_tradeshow", label:"\u{1F916} Work a Trade Show Booth", desc:"Industrial buyers in one room." },
8938 { key:"network", label:"\u{1F91D} Work the Network", desc:"Integrators and distributors." },
8939 { key:"negotiate", label:"\u{1F911} Renegotiate a Parts Contract", desc:"Trim recurring component cost." },
8940 ],
8941 finance: [
8942 { key:"v18_bigdep", label:"\u{1F3E6} Court a Large Depositor", desc:"Personally land a commercial deposit relationship." },
8943 { key:"v18_loanpitch", label:"\u{1F4CB} Originate a Loan Yourself", desc:"Write one good-quality loan by hand." },
8944 { key:"v18_reinsure", label:"\u{1F6E1}\uFE0F Negotiate Reinsurance", desc:"Cap your claims exposure for the year." },
8945 { key:"v18_regulator", label:"\u{1F3DB}\uFE0F Charm the Regulator", desc:"Smoother exams, fewer surprises, small reputation lift." },
8946 { key:"network", label:"\u{1F91D} Work the Chamber of Commerce", desc:"Deposits and loan leads." },
8947 { key:"negotiate", label:"\u{1F911} Renegotiate a Core Systems Contract", desc:"Trim a recurring cost." },
8948 ]
8949 };
8950 function founderActionsForIndustry(){

=== FILE: art-core.js  LINES 4776-4800
4776 const ind = c.industry||"ai_saas";
4777 const play = { retail:"open another store format and push the price tier up a notch once demand holds",
4778 restaurant:"add a location, but only after quality is above 60 \u2014 bad inspections are expensive",
4779 robotics:"finish the R&D program in flight, then add production capacity to clear backlog",
4780 finance:"raise deposits first, then lend up to ~80% of them at balanced underwriting",
4781 ai_saas:"pair a sales rung with a shipped feature \u2014 pipeline plus product beats either alone" }[ind];
4782 return `At ${fmt$(Math.round(rev))}/mo, your fastest lever in ${industry().name} is to ${play}.<br>Then reinvest: ${c.cash>rev*4? "you're sitting on cash, deploy it" : "fund it with the credit line rather than stalling"}.`;
4783 }
4784 if(/overpay|salary|payroll|cost|cut/.test(t)){
4785 const sorted = [...c.employees].sort((a,b)=>b.salary-a.salary).slice(0,3);
4786 const weak = c.employees.filter(e=>e.performance<45);
4787 return `Payroll is ${fmt$(c.employees.reduce((s,e)=>s+e.salary,0))}/mo. Top earners: ${sorted.map(e=>`${e.name} (${fmt$(e.salary)})`).join(", ")||"none"}.<br>${weak.length? `Underperformers to coach or cut: ${weak.map(e=>e.name).join(", ")}.` : "No clear underperformers \u2014 cutting here would cost you output."}`;
4788 }
4789 if(/equity|stock|option|cap table|vest|dilut/.test(t)){
4790 const e = eq(c);
4791 return `You hold ${(founderPct(c)*100).toFixed(1)}% of ${c.name} (${fmt$(stakeValue(c))} on a ${fmt$(c.valuation||0)} valuation). Option pool left: ${(100*e.pool/e.totalShares).toFixed(2)}%, ${e.grants.length} grant(s) outstanding, ${e.investors.length} investor(s) on the cap table.<br>Grants vest over 48 months and cut attrition \u2014 spend the pool on people you'd hate to lose.`;
4792 }
4793 if(/empire|second company|another business|ceo|step away/.test(t)){
4794 return `You run ${(G.companies||[]).length} company(ies); ${(G.companies||[]).filter(x=>x.passive).length} on autopilot. Personal cash ${fmt$(p.cash)}, total net worth ${fmt$(p.totalNetWorth||p.cash)}.<br>Hiring a CEO keeps a company compounding in the background while you start something in a different industry \u2014 diversification smooths the bad months.`;
4795 }
4796 if(/runway|burn|cash last|how long/.test(t)){
4797 if(runway===Infinity) return `Profitable \u2014 revenue ${fmt$(Math.round(rev))}/mo covers expenses. Cash ${fmt$(c.cash)}, plus ${fmt$(Math.max(0, debtCapacity()-c.debt.loanBalance))} of untapped borrowing capacity.`;
4798 return `About <b>${runway} month(s)</b> of runway at current burn. Cash ${fmt$(c.cash)}, credit available ${fmt$(c.debt.locLimit-c.debt.locBalance)}, additional borrowing capacity ${fmt$(Math.max(0, debtCapacity()-c.debt.loanBalance))}.`;
4799 }
4800 return assistantAnswerBase(question);

=== FILE: art-core.js  LINES 7636-7690
7636 }
7637
7638 /* ---------------- Line crews: dedicated staff per operating line ---------------- */
7639 const LINE_CREW_DEF = {
7640 retail: { dept:"sales", label:"Store Associate", noun:"store" },
7641 restaurant: { dept:"support", label:"Server / FOH", noun:"restaurant" },
7642 robotics: { dept:"operations", label:"Assembly Technician", noun:"production line" }
7643 };
7644 const LINE_REQ_CREW = {
7645 kiosk:2, shop:4, flagship:9, store:4, boutique:5,
7646 truck:3, bistro:8, fine:15,
7647 cell:4, line:10, plant:22
7648 };
7649 function opsLines(co){
7650 const o = (co||G.company).ops; if(!o) return [];
7651 return o.stores || o.locations || o.lines || [];
7652 }
7653 function lineReqCrew(l){
7654 if(LINE_REQ_CREW[l.key]) return LINE_REQ_CREW[l.key];
7655 if(l.baseDemand) return Math.max(2, Math.round(l.baseDemand/900));
7656 if(l.baseCovers) return Math.max(3, Math.round(l.baseCovers/260));
7657 if(l.capacity) return Math.max(4, Math.round(l.capacity/1.3));
7658 return 4;
7659 }
7660 function lineCrew(l){ return (G.company.employees||[]).filter(e=>e.lineId===l.id); }
7661 function lineStaffRatio(l){
7662 const req = lineReqCrew(l);
7663 const have = lineCrew(l).length;
7664 if(have===0) return 0.4; // skeleton crew from HQ staff
7665 return clamp(0.4 + 0.75*(have/req), 0.4, 1.2); // full crew ~1.15, overstaffed caps at 1.2
7666 }
7667 function lineStaffLabel(l){
7668 const r = lineStaffRatio(l), have = lineCrew(l).length, req = lineReqCrew(l);
7669 const cls = have>=req ? "good" : have >= req*0.6 ? "" : "bad";
7670 return `<span class="pill ${cls}">${have}/${req} crew \u00b7 ${Math.round(r*100)}% output</span>`;
7671 }
7672 function crewDefForOps(){ return LINE_CREW_DEF[(G.company.ops||{}).type] || null; }
7673 function hireLineCrew(l, qty){
7674 const def = crewDefForOps(); if(!def) return 0;
7675 const room = headcountCap() - G.company.employees.length - G.ui.queue.filter(q=>q.hireDept).length;
7676 qty = Math.min(qty, Math.max(0, room));
7677 if(qty<=0){ alert("Office/headcount cap is full \u2014 upgrade your workspace first."); return 0; }
7678 for(let i=0;i<qty;i++){
7679 queueAdd({ id:uid(), label:`Hire ${def.label} \u2014 ${l.name}`, hireDept:def.dept, hireRung:"rep", run:(report)=>{
7680 const e = hire(def.dept, "rep");
7681 e.lineId = l.id; e.lineName = l.name;
7682 report.push(`${e.name} joined ${l.name} as ${def.label}.`);
7683 }});
7684 }
7685 return qty;
7686 }
7687
7688 /* ---------------- Ops resolvers, now crew-aware ---------------- */
7689 function opsResolveRetail(co, report){
7690 const o = co.ops;

=== FILE: art-core.js  LINES 1730-1760
1730 wireBucketButtons(wrap);
1731 return wrap;
1732 }
1733
1734 function renderLadderBucket(deptKey){
1735 const ladder = LADDERS[deptKey];
1736 const cap = headcountCap();
1737 const total = G.company.employees.length;
1738 let cards = "";
1739 ladder.rungs.forEach((r)=>{
1740 const count = rungCount(deptKey, r.key);
1741 if(r.under){
1742 const capacity = rungCapacity(deptKey, r.key);
1743 if(capacity===0 && count===0) return; // not unlocked yet, hide entirely
1744 }
1745 const remaining = rungRemaining(deptKey, r.key);
1746 const roomLeft = Math.max(0, cap-total);
1747 const canHireMore = remaining>0 && roomLeft>0;
1748 const reqText = r.under ? `1 per ${r.per} ${LADDERS[deptKey].rungs.find(x=>x.key===r.under).label}(s) below · capacity ${rungCapacity(deptKey,r.key)}` : `Limited only by office headcount cap (${cap})`;
1749 cards += `<div class="ladder-card"><div class="row">
1750 <div><div class="title">${r.icon} ${r.label} <span class="muted">(${count} on team${remaining===Infinity?'':` / ${count+remaining} capacity`})</span></div>
1751 <div class="desc">${r.desc?r.desc+" · ":""}${reqText}</div>
1752 <div class="desc">Salary ~${fmt$(r.salaryRange[0])}–${fmt$(r.salaryRange[1])}/mo each.</div></div>
1753 <div class="flexrow">
1754 ${canHireMore? `<div class="stepper"><button data-step-minus="${deptKey}:${r.key}">-</button><span class="n" id="step_${deptKey}_${r.key}">1</span><button data-step-plus="${deptKey}:${r.key}">+</button></div>
1755 <button class="btn small" data-hire="${deptKey}:${r.key}">Queue Hire</button>`
1756 : (roomLeft<=0 ? `<span class="pill">Office full</span>`
1757 : (r.under ? `<button class="btn secondary small" data-fasttrack="${deptKey}:${r.key}">⚡ Fast-Track Hire (+${fmt$(Math.round(r.salaryRange[1]*2))})</button>` : `<span class="pill">No capacity yet</span>`))}
1758 </div></div></div>`;
1759 });
1760 // Strategic Order unlocks as soon as there's a Team Lead or above in the

=== FILE: art-core.js  LINES 4050-4075
4050 renderAll();
4051 });
4052 return wrap;
4053 }
4054
4055 /* ---------------- Org chart ---------------- */
4056 function directReportMap(deptKey){
4057 const rungs = LADDERS[deptKey].rungs;
4058 const byRung = {};
4059 rungs.forEach(r=> byRung[r.key] = deptEmployees(deptKey).filter(e=>e.rung===r.key));
4060 const reportsOf = {};
4061 for(let i=rungs.length-1; i>0; i--){
4062 const leaders = byRung[rungs[i].key];
4063 const below = byRung[rungs[i-1].key];
4064 if(!leaders.length || !below.length) continue;
4065 leaders.forEach(l=>{ if(!reportsOf[l.id]) reportsOf[l.id] = []; });
4066 below.forEach((e,idx)=> reportsOf[leaders[idx % leaders.length].id].push(e));
4067 }
4068 return { byRung, reportsOf, rungs };
4069 }
4070 function orgNodeHtml(e, reportsOf, depth){
4071 const reports = reportsOf[e.id] || [];
4072 const shares = eq().grants.filter(g=>g.empId===e.id).reduce((s,g)=>s+g.shares,0);
4073 const equityTag = shares? ` <span class="pill good">${(100*shares/eq().totalShares).toFixed(2)}% equity</span>` : "";
4074 const label = `${e.icon} <b>${e.name}</b> <span class="muted">\u00b7 ${e.role}</span>${equityTag}${reports.length?` <span class="muted">\u00b7 ${reports.length} direct report(s)</span>`:""}`;
4075 if(!reports.length) return `<div style="padding:4px 0 4px ${depth*14}px;font-size:12.5px;">${label}</div>`;

=== FILE: art-core.js  LINES 8267-8300
8267 const FACILITIES = [
8268 { key:"annex", name:"Office Annex", cost:150000, rent:9000, cap:150, icon:"\u{1F3E2}" },
8269 { key:"campus", name:"Corporate Campus", cost:750000, rent:38000, cap:900, icon:"\u{1F3DB}\uFE0F" },
8270 { key:"tower", name:"Headquarters Tower", cost:3200000, rent:135000, cap:3000, icon:"\u{1F3EC}" },
8271 { key:"global", name:"Global Campus Network", cost:12000000, rent:430000, cap:9000, icon:"\u{1F30D}" }
8272 ];
8273 function v17Migrate(){
8274 const c = G.company; if(!c) return;
8275 if(typeof c.marketingBudget !== "number") c.marketingBudget = 0;
8276 if(!c.mkt) c.mkt = { spendTotal:0, leads:0, mrrTotal:0, months:0, last:{ spend:0, leads:0, mrr:0 } };
8277 if(!c.facilities) c.facilities = [];
8278 if(!c.product) c.product = { maturity:5, features:[], inProgress:null, shipped:[] };
8279 if(!c.product.active) c.product.active = c.product.inProgress ? [ { ...c.product.inProgress } ] : [];
8280 if(!c.acquisitions) c.acquisitions = [];
8281 if(typeof c.acquiredShare !== "number") c.acquiredShare = 0;
8282 }
8283 function facilityCap(){ return (G.company.facilities||[]).reduce((s,f)=>s+f.cap, 0); }
8284 function facilityRent(){ return (G.company.facilities||[]).reduce((s,f)=>{ const r=Number(f&&f.rent); return s + (isFinite(r)? r : 0); }, 0); }
8285 function headcountCap(){
8286 let cap = HEADCOUNT_CAP[G.company.hqStage] || 8;
8287 if(deptHas("operations","manager")) cap += 5;
8288 if(deptHas("operations","coo")) cap += 15;
8289 return cap + facilityCap();
8290 }
8291 function facilitiesSection(){
8292 v17Migrate();
8293 const c = G.company;
8294 const owned = c.facilities || [];
8295 const box = el(`<div class="card mt14"><h3>\u{1F3D7}\uFE0F Facilities & Campus</h3>
8296 <div class="muted mb8" style="font-size:12px;">Headcount capacity: <b>${c.employees.length}</b> on team of <b>${headcountCap()}</b> (base office ${HEADCOUNT_CAP[c.hqStage]||8} + ${facilityCap()} from facilities). Facilities are permanent and stack, so you can buy several of the same kind. Combined facility rent: <b>${fmt$(facilityRent())}/mo</b>.</div>
8297 <div class="flexrow" style="gap:6px;flex-wrap:wrap;">
8298 ${FACILITIES.map(f=>`<button class="btn secondary small" data-v17fac="${f.key}">${f.icon} Build ${f.name} \u2014 ${fmt$(f.cost)} \u00b7 +${f.cap.toLocaleString()} seats \u00b7 ${fmt$(f.rent)}/mo</button>`).join("")}
8299 </div>
8300 ${owned.length? `<div class="mt8">${owned.map(f=>`<span class="pill good">${f.icon||"\u{1F3E2}"} ${f.name} \u00b7 +${f.cap.toLocaleString()} seats \u00b7 ${fmt$(f.rent)}/mo</span>`).join(" ")}</div>`

=== FILE: art-core.js
=== PATTERN: ops\s*:\s*\{\s*type\s*:\s*"[a-z_]+"

=== FILE: art-core.js
=== PATTERN: type\s*:\s*"(store|stores|restaurant|production|fleet|salon|chairs|line|lines)"
3433:  if(industryKey==="restaurant") return { type:"restaurant", locations:[], priceTier:1, quality:62, inspectionFails:0, lastNet:0, lastGross:0 };

=== FILE: 40-v428-trucking.js
=== PATTERN: (ops\.type|ops\s*=|"trucking"|trucks)
365:/* ---- Fleet Ops screen: buy trucks, assign lanes, see hauling + earnings ---- */
638:  { key:"auto", name:"Commercial auto & cargo", base:1400, per:0, perTruck:820, cover:0.88, deduct:10000, desc:"Accidents, cargo claims, roadside liability.", only:["trucking"] },
651:  var heads = (c.employees||[]).length, trucks = 0;
652:  try{ if(p.perTruck && window.__v426IsFleet && window.__v426IsFleet()) trucks = (v45S().units||[]).length; }catch(e){}
653:  return Math.round(p.base + heads*(p.per||0) + trucks*(p.perTruck||0));
1172:function isTruck(){ var c=C(); return !!(c && c.industry === "trucking"); }
1210:  if(!c.__v427) c.__v427 = { trucks:[], drivers:[], reps:[], offers:[], contracts:[], lastGross:0, lastCosts:0, lastNet:0, rows:[], hist:[], stamp:"" };
1212:  ["trucks","drivers","reps","offers","contracts","rows","hist"].forEach(function(k){ if(!Array.isArray(s[k])) s[k] = []; });
1247:    trucksNeeded: Math.max(1, Math.ceil(loads / perTruck)),
1288:  s.trucks.forEach(function(t){ if(t.driver === id) t.driver = null; });
1304:  var unit = { id:uid2(), type:equipKey, name:def.name + " #" + (s.trucks.filter(function(t){ return t.type === equipKey; }).length + 1),
1306:  s.trucks.push(unit);
1313:  var t = s.trucks.filter(function(x){ return x.id === truckId; })[0]; if(!t) return;
1317:  if(d.truck){ var old = s.trucks.filter(function(x){ return x.id === d.truck; })[0]; if(old) old.driver = null; }
1329:  say("<b>" + o.shipper + " signed.</b> You need " + o.trucksNeeded + " seated " + TRUCKS[o.equip].name.toLowerCase() + " truck(s) to cover it.");
1339:  var t = s.trucks.filter(function(x){ return x.id === truckId; })[0]; if(!t) return;
1356:  s.trucks.forEach(function(t){ budget[t.id] = capacityOf(t); t.last = null; });
1360:    s.trucks.forEach(function(t){
1389:  s.trucks.forEach(function(t){
1406:    log("\u26A0\uFE0F " + sf.k.shipper + ": " + sf.need + " load(s) went uncovered \u2014 " + money(sf.pen) + " in penalties. You need more seated " + TRUCKS[sf.k.equip].name.toLowerCase() + " trucks.");
1426:  s.rows = s.trucks.map(function(t){
1440:  if(s.trucks.length || s.reps.length){
1461:      if(c && (!co || co === c) && c.industry === "trucking" && c.__v427) base += num(c.__v427.lastGross, 0);
1476:  return s.trucks.filter(function(t){ return t.type === type && t.driver && !t.down; }).length;
1484:  s.trucks.forEach(function(t){ if(t.driver && !t.down) capacity[t.type] += TRUCKS[t.type].miles; });
1487:    + '<span class="sub">Last month: ' + money(s.lastGross) + ' hauled \u00b7 net ' + money(s.lastNet) + ' \u00b7 ' + s.trucks.length + ' truck(s), ' + s.drivers.filter(function(d){ return d.truck; }).length + ' seated</span></div>';
1509:  /* trucks */
1511:  var fleet = "<h3>Your trucks</h3>";
1512:  if(!s.trucks.length) fleet += '<div class="muted">No tractors yet. Buy one above.</div>';
1515:      + s.trucks.map(function(t){
1549:      var need = o.trucksNeeded, have = seatedBy(o.equip);
1576:        var t = s.trucks.filter(function(x){ return x.id === d.truck; })[0];
1602:  if(s.contracts.length && s.trucks.filter(function(t){ return t.driver; }).length) return null;
1605:    { done: s.trucks.length > 0, text:"Buy one tractor in Fleet Ops that matches the freight your rep sells." },
1607:    { done: s.trucks.filter(function(t){ return t.driver; }).length > 0, text:"Seat the driver in the truck on Fleet Ops." },
1723:function isTruck(){ var x=c(); return !!(x && x.industry === "trucking"); }
1908:function isTruck(){ var c = C(); return !!c && c.industry === "trucking"; }
2009:  st.trucks = st.trucks || [];
2032:  st.trucks.forEach(function(t){
2035:    st.trucks.forEach(function(x){ if(x.driverId) taken[x.driverId] = true; });
2081:      trucksNeeded: Math.max(1, Math.round(loads / 6)),

=== FILE: 50-v429-v432.js
=== PATTERN: (nail_salon|salon|agency|ops\.type)
1032:     robotics and finance do. SaaS, salon, trucking and agency do not. */

